name: Triggered Workflow
on:
  workflow_run:
    workflows: ["Hello World"]
    types: 
      - completed
jobs:
  triggered-workflow:
    runs-on: ubuntu-18.04
    steps:
    - name: Triggered
      run: |
        echo "I've been triggered ${{ secrets.greeting }} !"
